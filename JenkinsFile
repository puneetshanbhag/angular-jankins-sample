pipeline {
    agent any

    stages {
        stage('git clone') {
            steps {
                 git branch: 'main', url: 'https://github.com/puneetshanbhag/angular-jankins-sample.git'
            }
        }
        stage('Build') {
            steps {
                 sh "npm install"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}